window.VFX=(()=>{const P=[];const rings=[];const trails=[];function burst(x,y,c,n=18,pow=4){for(let i=0;i<n;i++){let a=Math.random()*Math.PI*2,s=Math.random()*pow+1;P.push({x,y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,life:.45+Math.random()*.35,max:.7,size:2+Math.random()*4,c})}}function ring(x,y,c,r=18){rings.push({x,y,c,r,a:1})}function trail(x1,y1,x2,y2,c){trails.push({x1,y1,x2,y2,c,a:1,w:10})}function update(dt){for(let i=P.length-1;i>=0;i--){let p=P[i];p.x+=p.vx;p.y+=p.vy;p.vy+=.12;p.life-=dt;if(p.life<=0)P.splice(i,1)}for(let i=rings.length-1;i>=0;i--){let r=rings[i];r.r+=220*dt;r.a-=2.7*dt;if(r.a<=0)rings.splice(i,1)}for(let i=trails.length-1;i>=0;i--){let t=trails[i];t.a-=5*dt;t.w*=.94;if(t.a<=0)trails.splice(i,1)}}function draw(ctx){ctx.save();for(const t of trails){ctx.globalAlpha=t.a;ctx.strokeStyle=t.c;ctx.lineWidth=t.w;ctx.shadowBlur=25;ctx.shadowColor=t.c;ctx.beginPath();ctx.moveTo(t.x1,t.y1);ctx.lineTo(t.x2,t.y2);ctx.stroke()}for(const r of rings){ctx.globalAlpha=r.a;ctx.strokeStyle=r.c;ctx.lineWidth=3;ctx.shadowBlur=20;ctx.shadowColor=r.c;ctx.beginPath();ctx.arc(r.x,r.y,r.r,0,7);ctx.stroke()}for(const p of P){ctx.globalAlpha=Math.max(0,p.life/p.max);ctx.fillStyle=p.c;ctx.shadowBlur=12;ctx.shadowColor=p.c;ctx.fillRect(p.x-p.size/2,p.y-p.size/2,p.size,p.size)}ctx.restore()}return{burst,ring,trail,update,draw}})();
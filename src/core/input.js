export class Input{constructor(){this.d=new Set;this.p=new Set;addEventListener('keydown',e=>{if(!this.d.has(e.code))this.p.add(e.code);this.d.add(e.code)});addEventListener('keyup',e=>this.d.delete(e.code))}held(k){return this.d.has(k)}use(k){let x=this.p.has(k);this.p.delete(k);return x}axis(){return(this.held('KeyD')||this.held('ArrowRight')?1:0)-(this.held('KeyA')||this.held('ArrowLeft')?1:0)}}
